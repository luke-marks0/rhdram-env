# Task config (SPEC §7/§10). Compiled by rowhammer_env/tasks/compiler.py.
id: ddr4_unknown_adjacency_v1
family: unknown_adjacency
profile: ddr4_vts25_v1
standard: DDR4
mitigation:
  name: none
  params: {}
disclosure:
  mapping: logical_only
  adjacency: candidate_set
  victim: row_handle
  profile: public_profile_id
  # Discovery family: the policy must narrow candidates by bank-conflict timing
  # (P22 §0.2), so it needs the issued-event trace + per-issue timing digest.
  feedback: full_trace
objective:
  type: target_row_flip
  target: row_handle
budgets:
  tool_calls: 200
  acts: 100000
  cycles: 5000000
reward: sparse_success
