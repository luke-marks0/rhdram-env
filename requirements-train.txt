# Install a matching CUDA build of PyTorch first on the training machine.
-r requirements.txt
torch>=2.9,<3
transformers==4.57.6
peft==0.18.0
accelerate==1.12.0
# W&B is optional: pip install wandb, then set wandb.enabled: true.
